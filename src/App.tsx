/**
 * App shell.
 * =========
 * This branch boots into "Nibble", the underwater fish game (see src/game).
 * The original CubeA warehouse dashboard components remain in the repo but are
 * no longer mounted here.
 */

import { GameApp } from "@/game/components/GameApp";

export default function App() {
  return <GameApp />;
}
