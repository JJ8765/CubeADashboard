node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
*.tsbuildinfo

# Never commit proprietary source workbooks / inventory exports.
# These are uploaded into the app at runtime, not stored in the repo.
*.xlsb
*.xlsx
*.xls
!src/**/*.sample.*
# Anchored to the repo root so proprietary data drops are ignored, but
# source directories named "data" (e.g. src/game/data) are still tracked.
/data/
/uploads/
